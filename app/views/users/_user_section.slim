ul
  - @user.printable_attributes.each do |attribute|
    li = "#{attribute} -> #{@user.send(attribute)}"

.row
  = button_to @user.active? ? 'Deactivate' : 'Activate', user_account_activations_path(@user), method: :put, remote: true